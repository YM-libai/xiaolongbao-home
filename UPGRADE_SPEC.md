# 个人名片页 · 「大神级」视觉升级需求(给 Codex)

## 任务
把现有的 `index.html`(个人名片+简历,基础版)升级为更精致、有"Awwwards 获奖个人站"气质的版本。
**只升级视觉与交互,不允许改动任何文案内容。** 保持**单文件、零依赖、纯原生**、可双击打开、可部署 GitHub Pages。

## 铁律(内容锁定,一个字符都不许变)
下列文案必须原样保留,只允许改 CSS/HTML 结构/JS,不得改文字:
- `小笼包`
- `东南大学在读 · AI 产品探索者 · 把想法做成真产品`
- `从 0 到 1，把好奇变成能用的东西`
- `现状` / `东南大学在读` / `「医学影像学 + AI」双学位`
- `正在备考转专业` / `自设「网络安全 + 法学」双学士`
- `作品` / `灵敏度工坊` / `无畏契约新手灵敏度引导工具 · 5 步找到适合自己的鼠标灵敏度`
- `纯前端单文件` / `已上线 GitHub Pages`
- `技能 / 方向` / `AI 应用与产品` / `Hermes 工作流` / `AI 产品方法论` / `网页开发入门` / `HTML / CSS 静态站` / `GitHub Pages 部署` / `网络与安全入门` / `备考方向` / `兴趣驱动` / `0 → 1 做产品` / `从想法到上线` / `完整闭环`
- `正在投入` / `备考网安二学位` / `探索 AI 产品`
- `暂不公开联系方式 · 欢迎交流`
- 作品卡外链 `https://ym-libai.github.io/lingmindu-gongfang/`(保持)

## 现有风格基础(保留底色)
深色玻璃拟态 + 紫 `#8b5cf6` / 青 `#22d3ee` 霓虹,标题渐变字。在这个基础上**升级**,不是推倒重来。

## 要模仿的大神特质(逐条落实)
1. **编辑排印感**:Hero 标题更大更有气场,加入字重/字距对比;区块标题更讲究;正文留白更从容。标题可做成"逐字/逐词浮现"的滚动入场动画。
2. **平滑滚动 + 滚动驱动动画**:整页滚动顺滑;每个区块进入视口时逐段浮现(fade-up),Hero 标题做**字符/词级 split 动画**(逐字从下往上出现),这是大神的招牌。
3. **微交互**:
   - 背景加一层**轻量 Canvas 星尘/光点粒子**,缓慢漂移,鼠标移动时有轻微视差/引力反馈(原生实现,不用 Three.js)。
   - 光标跟随一个柔和光晕(pointer glow)。
   - 作品卡 hover:渐变描边 + 箭头位移;技能卡 hover:边缘光斑。
   - 按钮/链接 hover 有触感反馈。
4. **preloader(加载屏)**:进站先显示品牌 mark「包」浮现 + 一条极短的光点进度,约 0.8~1.2 秒淡出,再放内容入场。
5. **背景深度**:紫青光斑继续,但叠加一层细腻网格 + 粒子星尘,避免死黑;整体更有"灵感/星云"气质(契合「小笼包」的灵气人设)。
6. **克制**:所有动效要 smooth、不吵;帧率流畅;不阻塞内容。

## 硬性要求
- 单文件 `index.html`,**零外部依赖**,不允许 CDN 引 GSAP/Three.js/Lenis;用原生 JS + Canvas/CSS。
- 内容不因预加载隐藏:preloader 结束后内容必然显示;加 `<noscript>` 兜底;`prefers-reduced-motion` 下关闭全部动画、内容直接可见。
- 深色主题、响应式(桌面 ~860px 居中、移动全宽)、`lang="zh-CN"`、保留 SEO/og/theme-color。
- 不出现:本地路径、凭据、内部词(缪斯/工作档案)。
- 移动端性能:粒子数量在移动端减半;动画尊重电池/性能。

## 交付
在 `D:\\HermesTeam\\xiaolongbao-home\\index.html` 生成升级版,浏览器打开无报错、滚动流畅。
