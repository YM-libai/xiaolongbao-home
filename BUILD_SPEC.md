# 个人名片 + 简历 · 单页静态网站（给 Codex 的需求）

## 目标
做一页「个人名片 + 简历」单页静态网站，纯前端、无构建工具、无后端。产出单个 `index.html`（内联 CSS + 少量 JS），可直接用浏览器打开，也能推到 GitHub Pages 供访问。

## 视觉基调（沿用「灵敏度工坊」的深色玻璃拟态气质，但更精致）
- 深色背景：径向渐变，主色 `#0a0a12 / #07070e`，加紫 `#8b5cf6` 与青 `#22d3ee` 的霓虹光晕（blur 环境光斑）
- 玻璃拟态卡片：半透明、细描边、backdrop-blur、柔和投影
- 渐变文字：标题高亮用紫→青渐变（background-clip:text）
- 字体：PingFang SC / Microsoft YaHei / system-ui，中文友好
- 响应式：桌面宽度居中限量（约 860px），移动端全宽，安全区适配
- 动效克制：fade-in 入场、卡片悬停微浮起、光晕缓慢漂移，不臃肿

## 页面结构与内容（访客视角，用中文）

### 1) Hero（顶部名片区）
- 大标题：「小笼包」用紫→青渐变高亮
- 一句话定位：`东南大学在读 · AI 产品探索者 · 把想法做成真产品`
- 一句副标语（可选，轻松一点）：如 `从 0 到 1，把好奇变成能用的东西`
- 顶部可放一个小的品牌徽标（渐变小方块 + 首字母，如「包」或「X」），复用灵敏度工坊 logo 风格

### 2) 现状 / 背景
- 列表式呈现，每项一个「标签 + 说明」：
  - 🎓 东南大学在读 · 「医学影像学 + AI」双学位
  - 🚀 正在备考转专业至「网络空间安全 + 法学」双学士
  - （如实写，不回避，但也别过度渲染）

### 3) 作品 / 项目（重点区）
- 一张「灵敏度工坊」作品卡：
  - 标题：灵敏度工坊
  - 一句话：无畏契约新手灵敏度引导工具 · 5 步找到适合自己的鼠标灵敏度
  - 技术/形式：纯前端单文件 · 已上线 GitHub Pages
  - 可外链到 `https://ym-libai.github.io/lingmindu-gongfang/`（给出视觉链接）

### 4) 技能 / 方向
- 四块小卡片，每块标题 + 两行说明：
  - 🧠 AI 应用与产品：Hermes 工作流、AI 产品方法论
  - 💻 网页开发入门：HTML / CSS 静态站、GitHub Pages 部署
  - 🛡️ 网络与安全入门：备考方向，兴趣驱动
  - 🔧 0→1 做产品：从想法到上线的完整闭环

### 5) 正在投入
- 一行收尾：`备考网法二次选拔 · 探索 AI 产品`
- 底部留一个淡淡的「暂不公开联系方式 · 欢迎交流」小字（低调，不放大）

## 硬性要求
- 单文件 `index.html`，双击即开，无外部依赖
- 中文简体，文案别油、别 AI 腔，要自然有灵气
- 响应式、移动优先，深色主题
- 不出现：真实本地路径、凭据、内部昵称（不用「缪斯/工作档案」这些）
- `lang="zh-CN"`，加 `theme-color`，加 SEO description

## 交付
在 `D:\\HermesTeam\\xiaolongbao-home\\` 目录生成最终可用的 `index.html`，确保浏览器打开无报错。
